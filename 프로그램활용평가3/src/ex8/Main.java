package ex8;

/*
 * 1. 외부에서 라이브러리를 다운 받은 후 사용하려는 패키지에 
 *    "libs" 등 라이브러리를 넣을 수 있는 폴더를 생성한다. 
 * 2. 다운받은 라이브러리를 복사해서 패키지 안에 생성한 폴더"libs"안에 넣는다.
 * 3. 폴더 안에 들어간 라이브러리를 우클릭 후 Build Path > Configure build path
 *    > Librart 탭에 Add jar 선택 후 libs안에 들어간 라이브러리를 선택해준다.    */
